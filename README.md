# Pytest Practice

## chapter 01

1. 用例之间相互隔离

2. 使用断言宣告结果

3. 为每个用例单独收集 IO 以及日志

4. 数据驱动测试、行为驱动测试、关键词驱动测试

5. 冒烟测试、步进测试、回归测试、增量测试


### 执行用例方法

1. 通过 pytest 命令执行

2. 通过启动函数

### 执行用例结果

1. 用例的执行环境

2. 用例的收集情况

3. 用例的执行情况

4. 用例的失败情况

5. 测试结果摘要

> 不会遍历 venv 和 `.` 开头的文件夹中的用例
> 遍历 test_ 开头和 _test 结尾的 .py 文件
> 遍历所有 Test 开头的类，不得拥有 `__init__` 方法
> 收集符合要求的函数和方法，test_ 开头，没有参数，返回值为 None

## Chapter 02

### 命令行参数

- `-h --help`：查看帮助信息
- `-x`：快速失败
- `-v`：增加输出详细程度
- `-s`：禁止 IO 捕获
- `-m`：根据 mark 筛选用例

### 配置文件


### 标记

实现筛选或者增强

- 用户标记：只能实现筛选
    1. 注册：在配置文件中注册
    2. 标记：通过 `@pytest.mark.xxx` 装饰器标记
    3. 筛选：命令行参数筛选
- 内置标记：
    1. skip：无条件跳过
    2. skipf：有条件跳过
    3. xfail：预期失败
    4. parametrize：参数化测试（数据驱动测试）
    5. usefixture：使用 fixture

## Chapter 03


### 核心功能

1. 前后置操作

2. 内容注入

### 创建 fixture

1. 创建函数

2. 添加 `@pytest.fixture` 装饰器

3. 添加 `yield` 关键词

### 使用 fixture

1. 必须使用用例进行调用 fixture

2. 通过标记或参数请求 fixture

3. 不允许直接调用 fixture

> Error 是 fixture 中报错，而 Failure 是用例中报错

### fixture 的共享范围

1. function：一个用例共享，默认级别

2. class：一个类共享

3. module：一个模块共享

4. package：一个包共享

5. session：一个测试会话共享，全局共享

6. conftest：跨文件共享，只会向上寻找，通过下级目录可以实现 fixture 注入

> fixture 具有面向对象特性

### 内容注入

通过 yield 返回，每个用例只能注入 1 次。
