# Pytest Practice

## chapter 01

1. 用例之间相互隔离

2. 使用断言宣告结果

3. 为每个用例单独收集 IO 以及日志

4. 数据驱动测试、行为驱动测试、关键词驱动测试

5. 冒烟测试、步进测试、回归测试、增量测试


### 执行用例方法

1. 通过 pytest 命令执行

2. 通过启动函数

### 执行用例结果

1. 用例的执行环境

2. 用例的收集情况

3. 用例的执行情况

4. 用例的失败情况

5. 测试结果摘要

> 不会遍历 venv 和 `.` 开头的文件夹中的用例
> 遍历 test_ 开头和 _test 结尾的 .py 文件
> 遍历所有 Test 开头的类，不得拥有 `__init__` 方法
> 收集符合要求的函数和方法，test_ 开头，没有参数，返回值为 None

## Chapter 02

### 命令行参数

- `-h --help`：查看帮助信息
- `-x`：快速失败
- `-v`：增加输出详细程度
- `-s`：禁止 IO 捕获
- `-m`：根据 mark 筛选用例

### 配置文件


### 标记

实现筛选或者增强